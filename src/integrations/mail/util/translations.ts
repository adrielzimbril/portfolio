import { appConfig } from "@/data/app-config";

const { defaultLocale } = appConfig.i18n;

export { default as defaultTranslations } from "@/integrations/i18n/translations/fr.json";

export { defaultLocale };
